# Test dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0

# Development dependencies  
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0

# Runtime dependencies (for testing)
aiohttp>=3.8.0
azure-identity>=1.12.0

# Azure dependencies (for testing Azure Blob Storage)
azure-storage-blob>=12.14.0
